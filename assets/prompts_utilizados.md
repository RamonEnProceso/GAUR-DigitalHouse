# Prompts Utilizados

---

## Backend

>Soy dev construyendo GAUR con Node.js + Express. Patrón MVC. ES modules.
>Genera la estructura completa con:
>- package.json con dependencias (express, cors, dotenv) y script start
>- index.js que levanta el servidor en el puerto 3000
>- src/app.js que configura Express, cors y monta las rutas
>- src/routes/index.routes.js con GET /api/health → { status: 'ok' }
>No generes modelos ni controllers todavía. Solo el esqueleto. Creamelo en Typescript. Usa async/await y comentarios breves en las funciones clave.

> "Soy dev construyendo GAUR con Node, Express y patrón MVC. La entidad principal es User que pertenece a un usuario.
>El modelo devuelve datos mockeados con al menos 3 registros de ejemplo — preparado para reemplazar con Supabase más >adelante. El controlador maneja: getAll, getById, create, update y remove. Las rutas usan userId hardcodeado >'user-123' por ahora. Todos los archivos usan ES modules."



---

## Prompt Open Claude

> "En base a los Briefs de la carpata `assets/DigitalHouse - Briefs IA.md`, termines de generar la aplicación."
